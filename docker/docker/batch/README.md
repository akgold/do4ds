This directory helps build the batch docker container. 

Last built with `docker built -t alexkgold/batch .`.

Output goes into `/project-out`, run with `docker run -v $(pwd):/project-out alexkgold/batch`
