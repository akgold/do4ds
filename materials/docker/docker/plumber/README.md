This directory is for building the plumber docker container.

Last built with `docker build -t alexkgold/plumber .`.

Output goes into /project-out, run with `docker run --rm -d -p 8000:8000 alexkgold/plumber`.
